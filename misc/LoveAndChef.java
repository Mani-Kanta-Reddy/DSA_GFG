package misc;

import java.util.Scanner;

public class LoveAndChef
{
    public static void main(String[] args)
    {
        Scanner sc = new Scanner(System.in);
        int T = sc.nextInt();
        while(T-- > 0) {
            int N = sc.nextInt();
            long limit = sc.nextLong();
            long[] arr = new long[N];
            for(int i = 0; i < N; i++) {
                arr[i] = sc.nextLong();
            }
            //logic

        }
    }
}
